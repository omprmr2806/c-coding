#include<stdio.h>
    int main(){
        float r,pi,area;
        printf("enter radius of circle:");
        scanf("%f", &r);
        pi=3.143;
        area= r*pi*r;
        printf("%f",area);    
        return 0;
    }
    