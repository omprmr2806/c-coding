#include<stdio.h>
    int main(){
        int a,b;
        printf("enter the first num:");
        scanf("%d",&a);
        printf("enter the second num: ");
        scanf("%d",&b);
        if(a>b)
        printf("a is the largest");
        else
        printf("b is the largest");
        return 0;
    }